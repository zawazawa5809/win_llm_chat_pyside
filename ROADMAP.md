# LLM チャットクライアント v1.0 実装ロードマップ

## ゴール定義（v1.0）

- 8GB クラスの Windows 端末で安定動作し、クラッシュやフリーズが例外的であること。
- LLM 応答までの待ち時間や失敗理由が、非エンジニアにも分かる程度に可視化されていること。
- 直近セッションの履歴が復元できること。
- 複数の社内 LLM / Ollama エンドポイントをプロファイルとして切り替え可能であること。
- 配布方法とアップデート手順が決まっており、展開時に迷わないこと。
- RAG / ファイル添付 / Web 検索などの重い機能は v1.0 スコープ外とする。

---

## バージョンマイルストーン一覧

- v0.1: 単一エンドポイント同期チャット MVP（済）
- v0.2: 安定化と基本 UX の底上げ
- v0.3: ストリーミング応答対応
- v0.4: 単一セッション永続化とエクスポート
- v0.5: 複数エンドポイント・プロファイル対応
- v0.6: 観測性とサポート性の向上
- v0.7: 配布・アップデート運用の確立
- v0.8: ネットワーク・認証周りの現実対応
- v0.9: バグ潰しとテスト・ドキュメント整備
- v1.0: 機能凍結＋安定版リリース

---

## v0.1（完了済）

**テーマ: 単一エンドポイント同期チャット MVP**

- PySide6 ベースのシンプルなウィンドウ

  - チャットビュー（QTextBrowser or QTextEdit 読み取り専用）
  - 入力欄（QPlainTextEdit）
  - 送信ボタン

- 単一エンドポイントへの同期 HTTP リクエスト
- Markdown 表示 (`setMarkdown`) による最低限の可読性
- 設定画面（base_url, model, api_key）とローカル保存

---

## v0.2: 安定化と基本 UX

**目的:** 「壊れにくい」「操作していてイラつかない」レベルへの引き上げ。

### 1. エラーハンドリング強化

- [x] 接続タイムアウト／全体タイムアウトの設定（例: 接続 5 秒、全体 30 秒）。
- [x] HTTP 4xx / 5xx / JSON パース失敗を分類し、ユーザー向けメッセージを出し分け。
- [x] 想定される例外をキャッチし、QMessageBox などで通知してアプリが落ちないようにする。

### 2. 送信中状態の管理

- [x] 送信中は送信ボタンと入力欄を disable。
- [x] 応答完了／エラー時に確実に enable に戻す。
- [x] ステータスバーやラベルで「送信中…」などの簡易インジケータを表示。

### 3. 基本操作性の改善

- [x] Ctrl+Enter で送信、Enter で改行のキーバインド実装。
- [x] 送信後に入力欄をクリアし、フォーカスを入力欄に戻す。
- [x] 新規メッセージ追加時に自動的に最下部までスクロール。

### 4. Markdown 表示の最低限チューニング

- [x] アプリ全体またはビュー単位で読みやすい日本語フォント・フォントサイズを指定。
- [x] 行間・パディングなどをスタイルシートで軽く調整し、詰まりすぎを解消。

**完了条件:**

- 想定される通信・設定エラーでアプリが落ちない。
- 10〜20 往復のチャットでも UI・操作感に大きなストレスがない。

---

## v0.3: ストリーミング応答対応

**目的:** 応答の体感速度と使い心地を改善する。

### 1. LlmClient インターフェース拡張

- [ ] 一括レスポンス用メソッドに加え、ストリーム用メソッドを追加：

  - 例: `iter_chat(messages: list[Message]) -> Iterator[str]`

- [ ] エンドポイントがストリーム非対応の場合はフォールバックで一括レスポンスを使用。

### 2. HTTP ストリーム処理

- [ ] chunked もしくは SSE のどちらか一方の方式をまずサポート。
- [ ] チャンクごとにテキストを取り出せるよう実装。

### 3. UI 側の逐次更新

- [ ] アシスタントの最新メッセージに対してチャンクを追記していく仕組みを実装。
- [ ] ストリーミング中も UI がフリーズしないよう、QThread / シグナル・スロットでメインスレッドをブロックしない。
- [ ] 可能であれば「応答停止」ボタンでストリームをキャンセルできるようにする（任意）。

**完了条件:**

- ストリーミング対応エンドポイントに対して、トークンが徐々に表示される。
- 応答が長くてもアプリが固まらない。

---

## v0.4: 単一セッション永続化とエクスポート

**目的:** 「昨日の会話を引き継げない」問題の解消。

### 1. ローカル履歴保存

- [ ] アプリ終了時に現在のセッションを JSON または Markdown 形式で保存。
- [ ] アプリ起動時に前回セッションを自動ロード。

### 2. 明示的なエクスポート

- [ ] 「この会話を Markdown で保存」メニュー／ボタンを追加。
- [ ] Markdown 形式で、ユーザーとアシスタントの区別が分かる形で書き出す。

### 3. 履歴サイズの簡易管理

- [ ] メッセージ数・総文字数にソフト上限を設け、大きくなりすぎた場合に警告表示。

**完了条件:**

- アプリを閉じて再度起動したとき、直近の会話が復元される。
- 会話を Markdown としてファイル保存し、エディタなどで参照できる。

---

## v0.5: 複数エンドポイント・プロファイル対応

**目的:** 社内 LLM / Ollama / 検証環境 / 本番環境など複数先切り替え。

### 1. 設定スキーマの拡張

- [ ] `profiles: [{ name, type, base_url, model, api_key, ... }]` 形式の配列に拡張。
- [ ] 既存の単一設定からの移行コード（マイグレーション）を用意。

### 2. UI のプロファイル選択

- [ ] メインウィンドウ上部にプロファイル選択用ドロップダウンを追加。
- [ ] 現在選択中のプロファイル名を常に表示。

### 3. プロファイル編集

- [ ] プロファイルの追加／編集／削除を行う設定ダイアログを実装。
- [ ] プロファイル変更時に LlmClient を再生成し、新設定を反映。

**完了条件:**

- 複数の接続先（例: 社内検証 / 社内本番 / ローカル Ollama）を保存し、簡単に切り替えられる。
- どのプロファイルを使っているか画面上から明確に分かる。

---

## v0.6: 観測性とサポート性

**目的:** 障害発生時に原因調査ができる状態を作る。

### 1. ログ出力（軽量）

- [ ] ログファイルに最低限の情報を記録：

  - タイムスタンプ
  - プロファイル名
  - リクエスト種別（メタ情報のみ、プロンプト本文は保存しない）
  - エラー発生時のメッセージ／スタックトレース

- [ ] ログ ON/OFF の設定を用意（既定 ON でよい）。

### 2. 簡易診断情報

- [ ] 「ログフォルダを開く」メニュー／ボタンを追加。
- [ ] 「アプリバージョン」「Python ランタイム」「OS バージョン」など診断情報を表示するダイアログを追加。

**完了条件:**

- 「何かおかしい」と言われた際に、ログと診断情報を見れば原因調査の足掛かりが得られる。
- プロンプト内容などセンシティブな情報をログに保存していない。

---

## v0.7: 配布・アップデート運用

**目的:** リリースのたびに配布方法で悩まない状態を作る。

### 1. ビルドプロセスの自動化

- [ ] PyInstaller 用スクリプト／spec ファイルを整備。
- [ ] バージョン番号をコード・ビルドアーティファクトで一元管理。

### 2. 配布形態の決定

- [ ] 単一 exe / ZIP / MSI など、社内で標準化しやすい形式を決める。
- [ ] 社内配布チャネル（ファイルサーバ, Intune, SCCM など）との連携方針を文書化。

### 3. アップデート運用

- [ ] 新バージョン公開時の周知・更新フローをドキュメント化。
- [ ] 自動アップデート機構は v1.0 時点では必須としない（任意）。

**完了条件:**

- 誰がビルドしても同じ成果物が安定して生成できる。
- 新バージョンを作成しても「配り方が分からない」状態にならない。

---

## v0.8: ネットワーク・認証周りの現実対応

**目的:** 社内ネットワーク／プロキシ／社内 CA などに現実的に耐える。

### 1. プロキシ対応

- [ ] OS の HTTP_PROXY / HTTPS_PROXY を尊重する実装。
- [ ] 必要に応じて設定画面からプロキシを明示指定できるようにする。

### 2. 認証情報の扱い

- [ ] 環境変数から API キーを読み込むオプション。
- [ ] 将来 Windows Credential Manager などに移行しやすい構造を意識した設計。

### 3. 証明書（社内 CA 等）対応

- [ ] 証明書検証無効化のような危険なスイッチは極力置かない。
- [ ] 必要であれば CA バンドルのパスを設定で指定できるようにする。

**完了条件:**

- 代表的な社内ネットワーク構成で、特別なカスタムビルドなしに通信できる。
- 認証情報をソースコードやログに平文で残していない。

---

## v0.9: 磨き込みとテスト・ドキュメント

**目的:** v1.0 前の最終調整フェーズ。

### 1. 既知バグの修正

- [ ] Issue ベースで既知の不具合を洗い出し、優先度の高いものから修正。

### 2. UI・文言の微調整

- [ ] メッセージ文言を見直し、非エンジニアにも伝わる表現に修正。
- [ ] 中途半端な機能や使われていない UI 要素を削る。

### 3. テスト整備

- [ ] client 層のユニットテスト拡充。
- [ ] ダミー LLM サーバを用意した簡易 E2E テスト（手動でも可）のシナリオ化。

### 4. ドキュメント

- [ ] 利用マニュアル（社内 Wiki 向け）。
- [ ] トラブルシューティング／FAQ。
- [ ] インストール手順・アンインストール手順。

**完了条件:**

- 想定ユースケースのテストシナリオをすべて実行しても致命的な問題が出ない。
- ユーザー向けと運用者向けの最低限のドキュメントが揃っている。

---

## v1.0: 安定版リリース

**目的:** 新機能追加を一旦止め、「使える安定版」として社内に展開する。

### 1. コードフリーズ

- [ ] バグ修正以外の変更を停止。

### 2. リリースノート作成

- [ ] 実装済み機能とスコープ外機能を明文化。
- [ ] 既知の制約・制限事項を列挙。

### 3. 利用条件の明示

- [ ] サポート対象 OS / 条件を明記。
- [ ] 「この使い方はサポートしない」範囲も含めて宣言。

### 4. 段階的ロールアウト

- [ ] 少人数パイロットユーザーで数週間運用し、フィードバックを収集。
- [ ] 必要があれば v1.0.1 / v1.0.2 パッチでバグ修正。

**完了条件:**

- 数週間の実利用で業務に支障をきたす問題が出ていない。
- 「他部署にも紹介してよい」と胸を張って言える状態になっている。

---

## v1.0 までにあえてやらないこと

- RAG（社内ドキュメント検索連携）
- ファイルアップロード・添付要約
- Web 検索連携
- 高度なテンプレ／プロンプト管理 UI
- テーマ切り替えや細かなカスタマイズ

これらは v1.1 以降のバックログ候補とする。
