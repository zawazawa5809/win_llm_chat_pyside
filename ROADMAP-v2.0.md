# LLM チャットクライアント v2.0 実装ロードマップ（RAG なし・軽量クライアント方針）

## 0. v2.0 ゴール定義

v2.0 で到達する状態を、RAG なし・8GB RAM 前提・軽量クライアントという制約の中で定義する。

- v1.0 時点の安定チャット機能・マルチプロファイル・配布運用が前提として安定している。
- 複数セッション（会話スレッド）を扱え、セッションに名前を付けて保存・再開できる。
- セッション単位で「案件／プロジェクト」などのワークスペースとして使える。
- 軽量なファイル連携（テキスト抽出＋要約／Q&A）ができる。
- セッション・添付テキストに対して、インデックスなしの簡易キーワード検索ができる。
- よく使うプロンプト・役割（役割プロファイル）を保存・再利用できる。
- 「常に最前面」ON/OFF と、グローバルホットキーによる起動／前面化が実装されている。
- セッション履歴・テンプレート・添付ファイルの扱いが整理され、データフローを説明できる。
- クライアントの常駐メモリはブラウザタブ 1〜2 枚程度以下を目安とし、不要な常駐処理やインデックス構築は行わない。

RAG／ベクタ DB／全社横断検索は v2.0 スコープ外とする。

---

## 1. バージョンマイルストーン概要

- v1.0: 安定版チャットクライアント（前提・実装済）
- v1.1: マルチセッション基盤（✅ 実装完了 2025-11-14）
- v1.2: プロンプトテンプレート & 役割プロファイル（次フェーズ）
- v1.3: グローバルホットキー & ウィンドウ呼び出し改善
- v1.4: 軽量ファイル添付（テキスト抽出＋要約/Q&A）
- v1.5: 軽量キーワード検索（セッション／添付テキスト）
- v2.0: UX・データ取り扱い整理＋ 2.x 安定版リリース

※ 「常に最前面 ON/OFF」は v1.0〜v1.1 の UX 改善として組み込む。

---

## 2. v1.0: 安定版チャットクライアント（前提）

**テーマ:** 単一セッション＋複数プロファイルの安定チャット基盤。

- 単一セッションのチャット（Markdown 表示・同期／ストリーミング対応）。
- エラーハンドリング・送信中ロック・基本ショートカット（Enter/Shift+Enter）。
- 設定画面（base_url, model, api_key, profile）とローカル保存。
- 複数エンドポイント（Ollama / 社内 LLM）プロファイル切り替え。
- 常に最前面 ON/OFF（ウィンドウフラグ＋設定保存）。
- PyInstaller による配布パッケージと基本的な配布手順の確立。

---

## 3. v1.1: マルチセッション基盤

**テーマ:** 「単一会話」から「複数会話」を扱える土台を作る。

**ステータス:** ✅ **実装完了** (2025-11-14)

### 機能

- Session モデルの導入

  - `Session`: `id`, `name`, `created_at`, `updated_at`, `messages[]`。
  - 実装: `src/win_llm_chat_pyside/models.py` に `Session` / `SessionMeta` dataclass 追加

- セッション一覧 UI

  - 左ペインにセッションリストを表示（`SessionListPanel`）。
  - 新規セッション作成／名前変更／削除ダイアログ統合。
  - 実装: `src/win_llm_chat_pyside/session_widgets.py` + `ui_main.py`

- 永続化

  - **JSON 採用**（SQLite ではなく、軽量性重視）。
  - 起動時にセッション一覧メタデータをロードし、選択セッションのみ全文ロード。
  - 実装: `src/win_llm_chat_pyside/session_repository.py` で `index.json` + `session_<id>.json` 管理

- 単一セッションからの自動移行

  - v1.0 までの旧単一セッション履歴を、初回起動時に自動検出＆移行。
  - 移行は 1 度限り。

- 設定 UI 拡張

  - 「設定...」ダイアログをタブ化（接続・チャット・履歴・表示・ネットワーク・ログ）。
  - `history_enabled` で履歴永続化を ON/OFF できるように UI 統合。
  - 実装: `ui_main.py` に統合 `SettingsDialog` (QTabWidget)

### 軽量ルール

- セッション数が増えた場合も「一覧はメタだけ・開いているセッションだけ全文ロード」でメモリ使用を抑える。
- セッション数が人間的に現実的な範囲（数十〜数百）を超える運用は想定しない。

### テスト

- pytest: 29/29 成功（test_session_manager.py + test_session_repository.py）
- UI 手動検証: 複数セッション作成・切替・再起動後の復元確認済み

---

## 4. v1.2: プロンプトテンプレート & 役割プロファイル

**テーマ:** 毎回同じ前置きプロンプトを書く無駄を削る。

**ステータス:** ✅ **実装完了** (2025-11-14)

### 機能

- プロンプトテンプレート

  - `title`, `body`（Markdown）を持つテンプレートを登録／編集／削除。
  - ボタン／ドロップダウンから選択して入力欄に挿入。
  - 実装: `src/win_llm_chat_pyside/prompt_template_store.py`, `prompt_template_dialog.py` + UI 統合

- 役割プロファイル（system prompt）

  - `name`, `system_prompt`（Markdown）を持つ。
  - セッション作成時にプロファイルを選択し、そのセッションの先頭 system メッセージとして適用。
  - 既存セッションでも役割プロファイルを変更可能（変更時は注意メッセージ）。
  - 実装: `src/win_llm_chat_pyside/role_profile_store.py`, `role_profile_dialog.py` + UI 統合

### 永続化

- テンプレート／役割プロファイルは JSON でローカル保存。
  - 実装: `src/win_llm_chat_pyside/prompt_repository.py` で `TemplateRepository` / `RoleProfileRepository` 管理

### エラーハンドリング・ログ

- JSON 読み書き失敗時のログを `app_logger` で記録
- UI エラーダイアログで失敗をユーザーに通知（`_log_and_show_error()`）
- トップレベル未処理例外は `app.py` で キャッチ＆通知

### テスト

- pytest: 関連テスト 13+ 成功（test_prompt_repository.py, test_prompt_template_store.py, test_role_profile_store.py, test_session_manager.py など）
- UI 手動検証: テンプレート・役割作成/編集/削除, セッション統合, 再起動後の復元確認済み

---

## 5. v1.3: グローバルホットキー & ウィンドウ呼び出し

**テーマ:** 生産性を底上げするが、OS フックによる不安定化を避けつつ慎重に導入する。

### 機能

- グローバルホットキー

  - 例: `Ctrl+Alt+Space` など、初期値を 1 つ決める。
  - ホットキー押下で「ウィンドウを前面化／最小化状態から復帰」。
  - 設定画面でホットキーの変更／無効化が可能。

- ウィンドウ動作

  - トグル挙動（表示中なら最小化／非表示、非表示なら前面化）。
  - 既存の「常に最前面」設定と矛盾しないよう動作設計。

### 安定化のための考慮

- OS レベルのホットキー登録・解除が確実に行われるよう、アプリ起動／終了時の処理を明示。
- ホットキー衝突時はユーザーに分かるエラー表示（別アプリと競合している旨）。
- 仮想デスクトップやリモートデスクトップでの挙動は「完璧さ」を求めず、致命的なバグがない範囲を目標とする。

---

## 6. v1.4: 軽量ファイル添付（テキスト抽出＋要約/Q&A）

**テーマ:** RAG なしで「ファイルを読ませる」ギリギリのラインまで。

### 対応ファイル

- 優先: PDF, テキスト, Markdown。
- 余力があれば: docx（軽量ライブラリを使用）。

### 処理フロー

1. ユーザーがファイルを添付（ボタン or ドラッグ&ドロップ）。
2. クライアント側でその場でテキスト抽出。
3. テキスト長をチェックし、閾値（例: 1〜2 万文字）超過時は警告を出して分割を促す。
4. 抽出テキスト＋ユーザー指示を LLM に送信（要約／Q&A）。

### UI

- セッションごとの添付ファイルリスト（ファイル名＋サイズ＋ページ数などのメタ情報）。
- 「このファイルを要約」「このファイルについて質問する」といった補助ボタン（任意）。

### 軽量ルール

- 添付ファイルはそのセッションのための一時的なテキストとして扱い、常駐インデックスは持たない。
- 大量ページのファイルや大量ファイルはサポート対象外とし、警告で明示する。

---

## 7. v1.5: 軽量キーワード検索（セッション／添付テキスト）

**テーマ:** インデックスなし・ベクタなしで完結する検索だけを提供する。

### 1) セッション内検索（Ctrl+F）

- 対象: 現在のセッションのメッセージテキスト全体。
- 入力キーワードを大文字小文字無視で検索。
- 一致箇所を順送り／逆送りで移動しつつハイライト表示。

### 2) セッション一覧検索

- 対象: セッション名＋冒頭数メッセージ分のプレーンテキスト。
- キーワード検索で「マッチするセッションのみをリスト表示」またはヒットバッジ表示。
- 検索時にのみストレージ（JSON/SQLite）から走査する。

### 3) 添付テキスト検索（オプション）

- 対象: 抽出済みテキストを持つ添付ファイル（そのセッション内）。
- キーワード検索で、どのファイルにヒットしたか簡易一覧を表示。
- クリックで該当ファイルの一部抜粋を LLM に投げる補助プロンプトを生成（任意）。

### 軽量ルール

- 全て「その場でフルスキャン」するだけで、インデックス構築はしない。
- セッション数・添付数が常識的な範囲に収まる前提で設計する（数千単位はスコープ外）。

---

## 8. v2.0: UX・データ取り扱い整理＋ 2.x 安定版

**テーマ:** 2 系の機能を締めて「仕事で安心して使える」状態にする。

### 1) データフロー整理

- セッション／テンプレート／役割プロファイル／添付テキストの保存場所・フォーマットをドキュメント化。
- ログにどの情報を出すか・出さないかを明確にする（プロンプト本文は原則出さない）。

### 2) UX 微調整

- 検索 UI（Ctrl+F、セッション検索）の配置と一貫したショートカット設計。
- グローバルホットキーと各種ショートカットの競合チェックと文言調整。
- 使われない・中途半端な機能の削除や統合。

### 3) テスト・ドキュメント

- チャット／マルチセッション／テンプレ・役割／ファイル添付／検索／ホットキーのテストシナリオ作成と実行。
- ユーザー向けガイド（v1.0 からの追加機能を中心に）。
- トラブルシューティング（よくあるエラーと対処法）。

### 4) 安定版リリース

- バグ修正以外の新機能追加を一旦停止（コードフリーズ）。
- 限定ユーザーでの数週間運用 → 必要に応じて 2.0.1 / 2.0.2 を切る。

---

## 9. v2.0 までにあえてやらないこと

- RAG（ローカル／サーバ問わず）
- ベクタ DB や全文検索エンジンの常駐インデックス
- 全社横断のナレッジ検索機能
- 自動ジョブランナー／タスク実行エージェント
- プラグインマーケット的な拡張機構

これらは v2.x 以降の検討対象とし、v2.0 では「軽量・チャット中心・仕事用ワークステーション」としての完成度に集中する。
